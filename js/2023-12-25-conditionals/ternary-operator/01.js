let isUserAllowed = true;

let userMessage;

// expression ? "if expression true" : "if expression is false"
console.log(isUserAllowed ? "Merhaba" : "Lütfen giriş yapın");

userMessage = isUserAllowed ? "Merhaba" : "Lütfen giriş yapın";

let cüzdan = 500;
let msg = cüzdan < 500 ? "İşlem başarılı" : "İşlem başarısız bakiye yükle";

console.log(msg);

let val = "burak";

console.log(typeof val);

console.log(
  typeof val == "string" ? "doğru tipte" : "yanlış tipte, string olmalı"
);

//typeof val == "string"
